
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Whisper 플랫폼의 HR 전문가 상세 프로필 (개인정보 보안 적용)",
      "properties": {
        "id": { "type": "string", "description": "Firebase UID" },
        "username": { "type": "string", "description": "사용자 닉네임" },
        "email": { "type": "string", "format": "email", "description": "이메일 (민감 정보)" },
        "name": { "type": "string", "description": "성함 (민감 정보)" },
        "company": { "type": "string", "description": "소속 회사" },
        "department": { "type": "string", "description": "소속 부서" },
        "jobTitle": { "type": "string", "description": "직함" },
        "phoneNumber": { "type": "string", "description": "휴대전화 (민감 정보)" },
        "role": { "type": "string", "enum": ["member", "mentor", "admin"], "description": "사용자 권한" },
        "registrationDate": { "type": "string", "format": "date-time" },
        "profilePictureUrl": { "type": "string" }
      },
      "required": ["id", "username", "email", "name", "company", "department", "jobTitle", "phoneNumber", "role", "registrationDate"]
    },
    "Question": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Question",
      "type": "object",
      "description": "HR 집단지성 피드 게시글",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "text": { "type": "string" },
        "userId": { "type": "string" },
        "userRole": { "type": "string" },
        "nickname": { "type": "string" },
        "category": { "type": "string" },
        "viewCount": { "type": "number" },
        "answerCount": { "type": "number" },
        "createdAt": { "type": "number" },
        "userProfilePicture": { "type": "string" }
      },
      "required": ["id", "title", "text", "userId", "nickname", "createdAt"]
    },
    "JobListing": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "JobListing",
      "type": "object",
      "description": "HR 전문 채용 공고 정보",
      "properties": {
        "id": { "type": "string" },
        "companyName": { "type": "string" },
        "title": { "type": "string" },
        "location": { "type": "string" },
        "experience": { "type": "string" },
        "education": { "type": "string" },
        "deadline": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "logoUrl": { "type": "string" },
        "category": { "type": "string" },
        "createdAt": { "type": "number" },
        "userId": { "type": "string" }
      },
      "required": ["id", "companyName", "title", "location", "deadline", "createdAt"]
    },
    "PrivateMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PrivateMessage",
      "type": "object",
      "description": "사용자 간 1:1 개인 쪽지",
      "properties": {
        "id": { "type": "string" },
        "senderId": { "type": "string" },
        "senderNickname": { "type": "string" },
        "receiverId": { "type": "string" },
        "receiverNickname": { "type": "string" },
        "content": { "type": "string" },
        "createdAt": { "type": "number" },
        "isRead": { "type": "boolean" }
      },
      "required": ["senderId", "receiverId", "content", "createdAt"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "HR 전문가 개인 프로필 정보 (보안 구역)"
    },
    "/questions/{questionId}": {
      "schema": { "$ref": "#/entities/Question" },
      "description": "공개 HR 지식 공유 피드"
    },
    "/jobs/{jobId}": {
      "schema": { "$ref": "#/entities/JobListing" },
      "description": "HR 채용 정보 데이터베이스"
    },
    "/messages/{messageId}": {
      "schema": { "$ref": "#/entities/PrivateMessage" },
      "description": "1:1 개인 쪽지 데이터"
    },
    "/admin_configuration/site_settings": {
      "schema": { "type": "object" },
      "description": "관리자 전용 사이트 구성 정보"
    }
  }
}
